# 2026-06-10 开发日志

## 已完成

- 高占用进程支持 CPU / 内存排序切换，并记住上次选择。
- 进程采集保留完整快照，确保两种排序都能得到正确的前 6 项。
- 高占用进程扩展为前 6 项，面板高度增加到 684pt。
- 面板升级扩高时保持顶部位置，新增空间优先向下展开。
- 支持自由拖动监测面板，保持固定尺寸并记住上次位置。
- 修复 SwiftUI 内容拦截无边框窗口背景拖动的问题，改用顶部标题区显式拖拽手势。
- 将标题拖动区改为原生 AppKit `performDrag`，确保无边框桌面面板可以可靠移动。
- 增加本地与全局鼠标事件双通道，解决桌面层面板事件被 Finder 接走时无法拖动的问题。
- 移除与全局拖动监听冲突的旧 `performDrag` 覆盖层，避免窗口位移竞争。
- 将窗口提升到 Finder 桌面图标层之上，修复趋势时间切换无法点击的问题。
- 屏幕或分辨率变化后自动将面板约束在可见区域。
- 修复窗口控制器继承 `NSObject` 后的初始化声明。
- 建立 Swift Package 和 macOS 原生应用骨架。
- 实现固定在主屏幕左下角的桌面层面板。
- 实现 CPU、内存、磁盘、网络、电池和进程采集。
- 实现 SQLite 分钟聚合、7 天历史和趋势图。
- 实现持续高负载状态与系统通知。
- 实现菜单栏、设置和登录启动开关。
- 建立产品、技术、设计、安全、开发和测试文档。
- 添加左下角定位和历史范围单元测试。
- 补充构建缓存忽略规则，并将 Swift Package 清单升级到 Swift 6。
- 修复 Swift 6 对窗口单例析构清理的并发安全检查问题。
- 修复 Swift Package 开发运行模式调用通知中心时的崩溃。
- 增加本地 `.app` 打包脚本，并区分开发预览与完整运行模式。
- 修复本地打包脚本在 bash 下的路径解析兼容性。
- 修复通知授权回调在 Swift 6 下触发的线程隔离崩溃。
- 根据视觉检查修正 CPU 独立状态文案，并补齐趋势图指标图例。

## 待办

- 在完成代码签名和安全评审后接入只读 XPC 传感器辅助程序。
- 增加真实 GPU 占用的稳定采集方案。
- 对本地 `.app` 进行多显示器人工验收。
- 补充长期运行资源占用和数据库容量测试。
